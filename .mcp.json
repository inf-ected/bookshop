{
    "mcpServers": {
        "laravel-boost": {
            "command": "docker",
            "args": [
                "compose",
                "-f", "docker-compose.yml",
                "-f", "docker-compose.dev.yml",
                "exec", "-T", "php",
                "php", "artisan", "boost:mcp"
            ]
        }
    }
}
